# Book settings
# Learn more at https://jupyterbook.org/customize/config.html

title: openEHR Workshop 2024
author: Sophie de Klerk
copyright: "2024 (CC-BY)"
logo: logo.svg
only_build_toc_files: true

# Force re-execution of notebooks on each build.
# See https://jupyterbook.org/content/execute.html
execute:
  execute_notebooks: false

# Define the name of the latex output file for PDF builds
latex:
  latex_documents:
    targetname: book.tex

# Add a bibtex file so that we can create citations
bibtex_bibfiles:
  - references.bib

# Information about where the book exists on the web
repository:
  url: https://github.com/sophiedkk/openEHR-Hanze  # Online location of your book
  path_to_book: workshop  # Optional path to your book, relative to the repository root
  branch: main  # Which branch of the repository should be used when creating links (optional)

# Add GitHub buttons to your book
# See https://jupyterbook.org/customize/config.html#add-a-link-to-your-repository
html:
  use_issues_button: false
  use_repository_button: true
  home_page_in_navbar: false
  favicon: "logo.svg"

# Advanced and power-user settings
sphinx:
  config:
    language: nl
  extra_extensions:
  - sphinx_inline_tabs